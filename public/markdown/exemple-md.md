# Titre H1 (Symbole #)
## Titre H2 (Symbole ##)
### Titre H3 (Symbole ###)

Voici un paragraphe de texte standard. En Markdown, un simple retour à la ligne ne suffit pas, il faut laisser une ligne vide pour créer un nouveau paragraphe.

---
*Ligne de séparation (Symbole ---)*

## Style de texte
- **Gras** : (Symbole `**texte**`)
- *Italique* : (Symbole `*texte*`)
- ~~Barré~~ : (Symbole `~~texte~~`)
- `Code en ligne` : (Symbole `` `code` ``)

## Listes
### Liste à puces (Symbole - ou *)
- Élément 1
- Élément 2
    - Sous-élément (Tabulation + -)

### Liste ordonnée (Symbole 1.)
1. Premier
2. Deuxième

## Citations (Symbole >)
> "Le code, c'est comme l'humour. Quand on doit l'expliquer, c'est qu'il est mauvais." - Cory House

## Liens et Images
- [Lien vers Google](https://google.com) : (Symbole `[nom](url)`)
- Image placeholder : (Symbole `![alt](url)`)

![Image de test](https://picsum.photos/600/300)

## Tableaux (Symboles | et -)
| Composant | Utilité |
| :--- | :--- |
| `route.params` | Récupérer l'ID |
| `fetch` | Charger le fichier |
| `v-html` | Afficher le rendu |

## Blocs de Code (Symbole ```)

```javascript
// Exemple de fonction
function helloMMI() {
  console.log("Le Markdown fonctionne !");
}
helloHi();